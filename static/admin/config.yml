backend:
  name: github
  branch: main 
  repo: Pintar-Pemenang-Asia/help.pintar.co
  commit_messages:
    create: Create {{collection}} “{{slug}}”
    update: Update {{collection}} “{{slug}}”
    delete: Delete {{collection}} “{{slug}}”
    uploadMedia: Upload “{{path}}" [skip ci]
    deleteMedia: Delete “{{path}}”
    openAuthoring: '{{message}}'

# These lines should *not* be indented
media_folder: "static/img" # Media files will be stored in the repo under static/images/uploads
public_folder: "/img/" # The src attribute for uploaded media will begin with /images/uploads

i18n:
  structure: multiple_folders
  locales: [en, id]
  default_locale: en

collections:
- name: blog
  label: "blog"
  folder: blog
  identifier_field: title
  extension: md
  widget: "list"
  create: true
  i18n: true
  slug: "{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
  fields:
    - { name: title, label: Title, widget: string, i18n: true }
    - { name: body, label: Body, widget: markdown }
    - { label: "Tags", name: "tags", widget: "list" }
    - label: "Authors"
      name: "authors" 
      widget: "list"
      fields:
        - { name: name, label: Name, widget: string }
        - { name: title, label: Title, widget: string } 
        - { name: url, label: URL, widget: string } 
        - { name: image_url, label: ImageURL, widget: string }
- name: degrees
  label: "degrees"
  folder: "docs/degrees"
  identifier_field: title
  extension: md
  widget: "list"
  create: true
  slug: "{{slug}}"
  fields:
    - { name: title, label: Title, widget: string }
    - { name: body, label: Body, widget: markdown }
    - { name: sidebar_position, label: Sidebar Position, widget: number, value_type: int }
    - label: "Tags"
      name: "tags"
      widget: "list"
- name: enterprise
  label: "enterprise"
  folder: "docs/enterprise"
  identifier_field: title
  extension: md
  widget: "list"
  create: true
  slug: "{{slug}}"
  fields:
    - { name: title, label: Title, widget: string }
    - { name: body, label: Body, widget: markdown }
    - { name: sidebar_position, label: Sidebar Position, widget: number, value_type: int }
    - label: "Tags"
      name: "tags"
      widget: "list"
- name: faq
  label: "faq"
  folder: "docs/faq"
  identifier_field: title
  extension: md
  widget: "list"
  create: true
  i18n: true
  slug: "{{slug}}"
  fields:
    - { name: title, label: Title, widget: string, i18n: true }
    - { name: body, label: Body, widget: markdown }
    - { name: sidebar_position, label: Sidebar Position, widget: number, value_type: int }
    - { label: "Tags", name: "tags", widget: "list" }
- name: skills
  label: "skills"
  folder: "docs/skills"
  identifier_field: title
  extension: md
  widget: "list"
  create: true
  slug: "{{slug}}"
  fields:
    - { name: title, label: Title, widget: string }
    - { name: body, label: Body, widget: markdown }
    - { name: sidebar_position, label: Sidebar Position, widget: number, value_type: int }
    - label: "Tags"
      name: "tags"
      widget: "list"
- name: prakerja
  label: "prakerja"
  folder: "docs/prakerja"
  identifier_field: title
  extension: md
  widget: "list"
  create: true
  slug: "{{slug}}"
  fields:
    - { name: title, label: Title, widget: string }
    - { name: body, label: Body, widget: markdown }
    - { name: sidebar_position, label: Sidebar Position, widget: number, value_type: int }
    - label: "Tags"
      name: "tags"
      widget: "list"